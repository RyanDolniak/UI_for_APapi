import BaseComponent from "@/lib/BaseComponent";
import React from "react";

export default class Views extends BaseComponent {
  public render(): React.ReactNode {
    return <div>ViewUpdate</div>;
  }
}