import React from "react";
import BaseComponent from "$lib/BaseComponent";

export default class RegisterStudent extends BaseComponent {
  public render(): React.ReactNode {
    return <div>RegisterStudent</div>;
  }
}
